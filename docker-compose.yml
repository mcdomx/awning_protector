services:
  awning-protector:
    container_name: awning-protector
    build: .
    ports:
      - "${APP_PORT:-8767}:8767"
    env_file: .env
    volumes:
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
